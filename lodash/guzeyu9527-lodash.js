var guzeyu9527 = {
  isNaN: function(val) {
      if (val !== val) {
          return true
      } else {
        return false
      }
  }
}

/**
 * 将数组分成多组，每组有size个元素
 * 
 * @param ary {array} 需要拆分的数组
 * @param size {Number} 拆分的大小
 * @param {Array[][]}
 */
chunk: function(ary, size = 1) {

}

compact:function(ary, )